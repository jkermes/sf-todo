{% extends 'base.html.twig' %}

{% block body %}

    <h1>Add task</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
        <input type="submit" value="Add" />
    {{ form_end(form) }}

{% endblock %}

